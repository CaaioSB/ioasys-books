import styled from 'styled-components'
import { flexbox, layout, position, space, border, color, background } from 'styled-system'

export const Box = styled.div(flexbox, layout, position, space, border, color, background)

export default Box
